# Kernel launch counts

One whole language model per case — the pretrained checkpoints' exact topology on random weights — generated by `./kernels.sh` on 2026-08-18. Each cell is the number of GPU kernels launched by one iteration of that case; lower is better.

Counts are exact and repeatable — they follow from the op graph, not from the machine — so one iteration per case is measured rather than a criterion sample. They are read from cubecl's own per-sync profiling summary, which serialises the queue: the counts are meaningful, the timings in `target/kernel-logs/` are not.

| run | configuration |
|---|---|
| `cuda` | `batch=1 sequence=256 warmup_iters=1 budget=60s samples=10 simd=sse2 backend=dispatch<cubecl<cuda>> models=["mamba3-mimo", "mamba3-siso", "mamba2", "mamba1"]` |
| `cuda-fusion-autotune` | `batch=1 sequence=256 warmup_iters=1 budget=60s samples=10 simd=sse2 backend=dispatch<fusion<cubecl<cuda>>> models=["mamba3-mimo", "mamba3-siso", "mamba2", "mamba1"]` |

## `forward` — one chunkwise pass over the whole prompt (`run_parallel`)

| case | cuda | cuda + fusion + autotune |
|---|---|---|
| `mamba1` | 31928 | 19184 |
| `mamba2` | 2864 | 2072 |
| `mamba3-siso` | 2900 | 1748 |
| `mamba3-mimo` | 4040 | 2396 |

## `step` — one recurrent decode step (`run_sequential`)

| case | cuda | cuda + fusion + autotune |
|---|---|---|
| `mamba1` | 1137 | 534 |
| `mamba2` | 1257 | 870 |
| `mamba3-siso` | 1796 | 1198 |
| `mamba3-mimo` | 1916 | 1246 |
